<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.falcons">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".checklist.CheckListMenu" />
        <activity android:name=".checklist.CheckListAntes" />
        <activity android:name=".checklist.CheckListData" />
        <activity android:name=".checklist.CheckListMotorista" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".FerramentasActivity" />
        <activity android:name=".PdfActivity" />
        <activity android:name=".InstrucaoActivity" />
        <activity android:name=".manutencao.ManutencaoActivity" />
        <activity android:name=".manutencao.ManutencaoMenu" />
        <activity android:name=".classesExtras.Load">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>